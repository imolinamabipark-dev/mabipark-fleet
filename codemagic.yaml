workflows:
  android-apk:
    name: Mabipark Fleet — Android APK
    max_build_duration: 30
    environment:
      java: 17
      node: 22
    scripts:
      - name: Instalar dependencias
        script: npm install

      - name: Copiar socket.io a www/js
        script: node scripts/copy-vendor.js

      - name: Agregar plataforma Android
        script: |
          if [ ! -d "android" ]; then
            node node_modules/@capacitor/cli/bin/capacitor add android
          fi

      - name: Sincronizar web con Android
        script: node node_modules/@capacitor/cli/bin/capacitor sync android

      - name: Build APK
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

    artifacts:
      - android/app/build/outputs/apk/debug/app-debug.apk

    publishing:
      email:
        recipients:
          - tu-email@gmail.com
        notify:
          success: true
          failure: true